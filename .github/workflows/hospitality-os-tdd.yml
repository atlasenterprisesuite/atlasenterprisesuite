name: ATLAS Hospitality OS TDD

on:
  push:
    branches: ["feat/hospitality-os-core"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

concurrency:
  group: hospitality-os-tdd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  focused:
    name: Hospitality Core focused TDD
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Runtime
        run: |
          node --version
          npm --version
      - name: Install
        run: npm ci
      - name: Hospitality core contracts
        run: npx vitest run tests/unit/hospitality-core-types.test.ts tests/unit/hospitality-scope.test.ts tests/unit/hospitality-permissions.test.ts tests/unit/hospitality-core-events.test.ts tests/unit/hospitality-context.test.ts tests/integration/hospitality-os-boundaries.test.ts tests/integration/hospitality-os-routes.test.tsx --environment jsdom
      - name: Typecheck
        run: npm run typecheck
      - name: Unit suite
        run: npm run test:unit
      - name: Integration suite
        run: npm run test:integration
      - name: Production build
        run: npm run build
