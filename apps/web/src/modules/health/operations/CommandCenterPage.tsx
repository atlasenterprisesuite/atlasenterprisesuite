import { commandCenterMetrics, healthAlerts, healthModuleCatalog } from '../healthDomain';
import { HealthDataNotice } from '../shared/HealthDataNotice';
import { HealthModuleCard } from '../shared/HealthModuleCard';

export function CommandCenterPage() {
  return <section className="health-stack command-center"><HealthDataNotice state="demo" text="Values below are derived from ATLAS demonstration datasets, not AdventHealth production systems." /><div className="command-center-hero"><div><p className="eyebrow">ATLAS Health Intelligence Layer</p><h1>Smart Health Command Center</h1><p>One governed operational view across the ATLAS Health ecosystem.</p></div><div className="command-orbit" aria-hidden="true"><span>A</span><i /><b /></div></div><div className="command-layout"><aside className="command-rail"><h2>Operational Pulse</h2>{commandCenterMetrics.map(metric => <article key={metric.id} className="command-metric"><span>{metric.label}</span><strong>{metric.value}{metric.unit ?? ''}</strong><small>Demo source</small></article>)}</aside><div className="command-core"><div className="command-core-map"><span className="command-grid-glow" /><div><strong>ATLAS Health</strong><small>Connected module graph</small></div></div><div className="health-module-grid" aria-label="Health module portfolio">{healthModuleCatalog.map(module => <div key={module.id} data-testid="health-module-card"><HealthModuleCard title={module.name} description={module.description} to={module.route} /></div>)}</div></div><aside className="command-rail"><h2>Alerts</h2>{healthAlerts.map(alert => <article key={alert.id} className={`command-alert command-alert-${alert.severity}`}><span>{alert.severity}</span><strong>{alert.title}</strong><small>{alert.moduleId}</small></article>)}<article className="command-rail-card"><span>Environment</span><strong>DEMO</strong><small>No live hospital feeds</small></article></aside></div></section>;
}
