function fail(code,status=400,details={}){return Object.assign(new Error(code),{code,status,...details});}
function uuid(value){return /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(String(value||''));}
function bearerOf(request){const raw=request.headers.get('authorization')||'';if(!/^Bearer\s+\S+/i.test(raw))throw fail('authentication_required',401);return raw;}
async function readJson(response,code){if(!response.ok)throw fail(code,response.status>=500?502:response.status);return response.json();}
export async function resolveIntelligenceContext({request,supabaseUrl,publishableKey,fetchFn=fetch}={}){if(!request||!supabaseUrl||!publishableKey)throw new TypeError('auth_dependencies_required');const bearer=bearerOf(request),base={apikey:publishableKey,authorization:bearer,'content-type':'application/json'};let userResponse;try{userResponse=await fetchFn(`${supabaseUrl}/auth/v1/user`,{headers:base,cache:'no-store'});}catch{throw fail('identity_unavailable',502)}if(!userResponse.ok){if(userResponse.status===401||userResponse.status===403)throw fail('authentication_required',401);throw fail('identity_unavailable',502)}const user=await userResponse.json();if(!user?.id)throw fail('authentication_required',401);const requested=(request.headers.get('x-atlas-org-id')||'').trim();if(requested&&!uuid(requested))throw fail('invalid_input',400,{field:'organization_id'});let membershipsResponse;try{membershipsResponse=await fetchFn(`${supabaseUrl}/rest/v1/organization_members?select=org_id,role,status&user_id=eq.${encodeURIComponent(user.id)}&status=eq.active`,{headers:base,cache:'no-store'});}catch{throw fail('identity_unavailable',502)}const memberships=await readJson(membershipsResponse,'identity_unavailable');if(!Array.isArray(memberships)||!memberships.length)throw fail('active_organization_required',403);const membership=requested?memberships.find(row=>row.org_id===requested):memberships[0];if(!membership)throw fail('organization_membership_required',403);let permsResponse;try{permsResponse=await fetchFn(`${supabaseUrl}/rest/v1/identity_role_permissions?select=permission_code&role=eq.${encodeURIComponent(membership.role)}`,{headers:base,cache:'no-store'});}catch{throw fail('identity_unavailable',502)}const rows=await readJson(permsResponse,'identity_unavailable');const permissions=[...new Set((Array.isArray(rows)?rows:[]).map(r=>String(r.permission_code||'')).filter(Boolean))];if(!permissions.includes('intelligence.use')&&!permissions.includes('*'))throw fail('permission_denied',403,{permission:'intelligence.use'});const sessionHeader=(request.headers.get('x-atlas-session-id')||'').trim();return {context:{organization_id:membership.org_id,user_id:user.id,permissions,roles:[membership.role],session_id:sessionHeader||crypto.randomUUID(),request_id:crypto.randomUUID()},bearer};}
