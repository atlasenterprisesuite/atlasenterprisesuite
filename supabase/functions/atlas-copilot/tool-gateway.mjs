function stable(value){if(Array.isArray(value))return`[${value.map(stable).join(',')}]`;if(value&&typeof value==='object')return`{${Object.keys(value).sort().map(key=>`${JSON.stringify(key)}:${stable(value[key])}`).join(',')}}`;return JSON.stringify(value);}
function hash(value){let h=2166136261;for(const ch of value){h^=ch.charCodeAt(0);h=Math.imul(h,16777619);}return`atlas_${(h>>>0).toString(16).padStart(8,'0')}`;}
function has(context,permission){const permissions=Array.isArray(context?.permissions)?context.permissions:[];return permissions.includes('*')||permissions.includes(permission);}
function normalize(raw={}){const tool_name=String(raw.tool_name||'').trim();const args=raw.arguments&&typeof raw.arguments==='object'?structuredClone(raw.arguments):{};const required_permissions=Array.isArray(raw.required_permissions)?[...new Set(raw.required_permissions.map(String))]:[];const core={tool_name,arguments:args,risk_class:String(raw.risk_class||'read-only'),required_permissions,side_effect:String(raw.side_effect||'none'),cost_class:String(raw.cost_class||'none')};return {...core,provider:String(raw.provider||'unknown'),proposal_hash:hash(stable(core))};}
export function createToolGateway(){return Object.freeze({evaluate({proposals=[],context={}}={}){const accepted=[],approval_required=[],denied=[],seen=new Set();for(const raw of proposals){const item=normalize(raw);if(!item.tool_name){denied.push({...item,decision_reason:'invalid_tool'});continue;}if(seen.has(item.proposal_hash))continue;seen.add(item.proposal_hash);if(item.required_permissions.some(permission=>!has(context,permission))){denied.push({...item,decision_reason:'permission_denied'});continue;}const sensitive=item.risk_class!=='read-only'||item.side_effect!=='none'||item.cost_class!=='none';if(sensitive){approval_required.push({...item,decision_reason:item.cost_class!=='none'?'cost_or_side_effect_approval_required':'side_effect_approval_required'});continue;}accepted.push({...item,decision_reason:'policy_allowed'});}return {accepted,approval_required,denied};}});}
